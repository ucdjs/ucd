/**
 * Internal exports for advanced use cases.
 *
 * These are not part of the public API and may change in future versions.
 * Prefer using the main API via `loadPipelineFile` and `findPipelineFiles`.
 */
