import { describe, expect, it } from "vitest";
import {
  createUCDStore,
  UCDStore,
  type UCDStoreOptions,
} from "../src/store";

describe("todo", () => {
  it("todo", () => {
    expect(true).toBe(true);
  });
});
