# @ucdjs/utils v1.0.0
**Previous version**: `0.2.0`
**New version**: `1.0.0`


## ✨ Features
- **shared**: migrate utilities to @ucdjs-internal/shared (`4d7588fd`)
- **fs-bridge**: add HTTP and Node file system bridges with comprehensive tests (`5bc90ebc`)
- **fs-bridge**: implement recursive directory listing in HTTPFileSystemBridge (`b3ee5dd3`)
- **fs-bridge**: enhance listdir functionality with recursive support (`7db08f77`)
- **utils**: add flattenFilePaths function to recursively flatten file structures (`da32ab48`)
- **ucd-store**: add tests for mirrorUCDFiles and validateUCDFiles functions (`3214f78e`)
- **env**: add environment package with configuration and validation utilities (`2743f549`)
- **proxy**: add utility dependencies and improve error handling (`39eca3dc`)
- **fetch**: initialize fetch package with API client and documentation (`c4ec17fd`)
- **cli**: add &#39;dev:api&#39; script for API development (`b9c3b2ba`)
- **utils**: add constants for Unicode and UCDJS API URLs (`63b468f9`)
- **filter**: enhance `PathFilterFn` to accept additional filters (`d9cf1fc6`)
- **filter**: enhance path filter functionality (`b1929622`)
- **fs-bridge**: update rm method to default to non-recursive and non-force (`6ad0a2c2`)
- **utils**: add comprehensive tests for HTTPFileSystemBridge (`58d9abe8`)
- **utils**: enhance HTTPFileSystemBridge with new methods (`5ac936b2`)
- **utils**: implement Node.js file system operations (`f9c79acf`)
- **utils**: extend FileSystemBridge interface with new methods (`778da095`)
- add new fs impl (`8a09ff29`)
- add new vfs module (`3bf85d86`)
- add optional proxy URL to MirrorOptions and internal functions (`3292668c`)
- enhance validateUCDFiles to filter out directories and update test to reflect changes (`ef300d36`)
- update readdir method to support recursive option and modify validateUCDFiles to handle directories (`30e5440e`)
- update validateUCDFiles to return missing and not required files (`605ff972`)
- add TODO to validateUCDFiles for returning extra files (`95d54a2c`)
- add validateUCDFiles function and update FSAdapter interface with exists and readdir methods (`470abb64`)
- enhance mirrorUCDFiles to handle errors and return located files (`febe7ab6`)
- enhance mirrorUCDFiles to handle errors and return located files (`b7e05f61`)
- enhance mirrorUCDFiles with pattern matching and error handling (`73c02513`)
- add dependencies for unicode utilities and defu in package.json and pnpm-lock.yaml (`b7e31ff9`)
- implement mirrorUCDFiles (`119a25b9`)
- add UCD file mirroring utilities and filesystem adapter (`e85bdbde`)
- add turbo.json configuration files for cli, schema-gen, ucd-store, and utils; update tsconfig.base.build.json and remove test:watch task from turbo.json (`48dad498`)

## 🐛 Bug Fixes
- **utils**: ensure &#39;entries&#39; is an array before processing (`54af41b9`)
- **fs-bridge**: set default baseUrl for HTTPFileSystemBridge (`0a699f21`)
- **fs-bridge**: ensure baseUrl is always defined (`0753a323`)
- **flatten**: correct file path handling in flattenFilePaths function (`91aff454`)
- **utils**: update predefined filter patterns for exclusions (`c39fa797`)
- **utils**: update listdir to return file names and enhance recursive listing (`f4f97aca`)
- throw if unresolved import (`8123dda2`)
- **tests**: update API URLs in mirror and validate test files to use UCDJS_API_BASE_URL (`8c7678e3`)
- **tests**: update API URLs in test files (`9dff312a`)
- **utils**: use UNICODE_PROXY_URL as default base URL (`a26f9757`)
- **constants**: update default Unicode API base URL (`2415c8e1`)
- **constants**: handle process.env safely in constants file (`1dca07f2`)
- correct mock reset method in validate test and add ucd-files to hidden logs (`6807b611`)

## 📝 Documentation
- **utils**: enhance createPathFilter documentation with detailed examples (`f0308f87`)
- **utils**: update README to enhance usage examples and clarify functionality (`82a55e1c`)

## ♻️ Refactoring
- reorganize pnpm catalogs for better scoping (##480) (`ba721776`)
- **path-utils, shared-ui, shared, utils**: update exports to use explicit file paths (`c2a39fbf`)
- **shared-ui**: reorganize component imports to ui directory (`c64c288f`)
- **utils**: remove unused type `TreeEntry` from exports (`f1e82e6c`)
- **tsdown-config**: update package references to @ucdjs-tooling/tsdown-config (`ccc002da`)
- update tsconfig references to use @ucdjs-tooling/tsconfig (`e5c39ac8`)
- migrate `flattenFilePaths` imports from `@ucdjs/utils` to `@ucdjs/shared` (`49318725`)
- remove flatten utility and related tests (`4db6f390`)
- **shared, utils**: move `safeJsonParse` function to shared package (`ee893aa4`)
- **fs-bridge**: update listdir return type to FSEntry[] (`ef8651d0`)
- **fs-bridge**: enhance write operation to create parent directories (`793b0b1c`)
- **fs-bridge**: remove unused `getSupportedBridgeCapabilities` function (`1d559db3`)
- **fs-bridge**: enhance type safety in FileSystemBridge interfaces (`e333dd24`)
- **fs-bridge**: improve type safety and structure of file system bridge interfaces (`12673785`)
- **fs-bridge**: enhance HTTP and Node file system bridge implementations (`da79ce61`)
- **fs-bridge**: simplify interface and enhance type safety (`c784e16e`)
- **store**: update file tree types and import flattenFilePaths from utils (`c3cb6344`)
- rename FileTreeNode to UnicodeTreeNode and update related schemas (`7f366e53`)
- **api**: update type references in flatten functions (`85d91251`)
- **store**: update default base URL for remote store (`add5c0a7`)
- **store**: streamline store initialization and manifest handling (`de5eb013`)
- **utils**: update `PathFilter` methods for improved filter management (`64e60659`)
- **store**: update filter type and enhance file retrieval methods (`cf7f7b1d`)
- **store**: replace hardcoded URLs with constants and improve store initialization (`3ea42a18`)
- **store**: unify UCDStore implementation and remove legacy classes (`5a9bea8d`)
- remove local and remote UCD store implementations (`29f0f599`)
- remove deprecated FsInterface tests and related code; implement repairUCDFiles functionality with improved error handling and file downloading logic (`9d37fafe`)
- remove FsInterface and related functions; update download logic to use buildUCDPath (`d3ee25dc`)
- replace internal flattening function with helper utility (`3610c72d`)
- migrate ucd-store to use utils (`cadc55b4`)
- replace hardcoded proxy URL with configurable option in internal__processEntries (##58) (`f03d9ff2`)
- improve documentation for validateUCDFiles function (`4c713242`)
- ucd-files impl (`b1d799a4`)
- simplify createDefaultFSAdapter and add tests for FS adapter (`3fccaf55`)
- move filter functionality from ucd-store to utils package (`98b84b07`)

## ✅ Tests
- **ucd-store**: integrate `memfs` for enhanced filesystem operations (`1b539ab2`)
- **fs-bridge**: add comprehensive tests for HTTPFileSystemBridge operations (`0d036046`)
- **fs-bridge**: add comprehensive tests for defineFileSystemBridge (`505086fc`)
- remove obsolete HTTP and Node file system bridge tests (`587dd435`)
- **utils**: enhance UnicodeTree structure in flattenFilePaths tests (`0e8b8863`)
- **utils**: enhance directory listing tests and add stat operation validation (`eb6ccf00`)
- **filter**: add support for `extraFilters` parameter in `createPathFilter` (`2dd5f6f5`)
- **utils**: enhance custom pattern matching using createPathFilter (`5f892755`)
- **utils**: extend functionality to support dynamic filter patterns (`5af9d48d`)
- update missing files assertion to use arrayContaining and check length (`1bc63d64`)
- add comprehensive tests for mirroring (`7da86225`)

## 🔧 Chores
- **workspace**: update dependencies and package manager (`1fcda2ca`)
- reorganize package catalogs and update dependencies (`ea2df11e`)
- update packageManager to pnpm@10.29.1 across all packages (`6bb966ab`)
- update dependencies and package manager (`e91a1ec4`)
- update dependencies (`59402701`)
- update pkg (`b4039996`)
- update dependencies (`51e6a071`)
- update typecheck command in package.json files (`34fa0ae7`)
- upgrade pnpm (`b06a7dd7`)
- update dependencies (`4b3590b9`)
- **deps**: update dependencies and package manager version (`8c5f051f`)
- **deps**: update package versions in `pnpm-workspace.yaml` (`2cca2fdf`)
- **deps**: update package versions in `pnpm-workspace.yaml` and package.json files (`34f3cab1`)
- **release**: 📦 version packages (`d592b87c`)
- update pnpm (`62648fcd`)
- update pnpm (`7e789f64`)
- update dependencies (`bf3b20f8`)
- update packageManager to pnpm@10.16.1 across all packages (`ec4ebd9d`)
- update package versions in pnpm-workspace.yaml to remove caret (^) for consistency (`8521f03a`)
- use node 22.18 (`2a9bfcd7`)
- fix (`ec3a473b`)
- update dependencies (`c813c448`)
- update build scripts to include custom TypeScript configuration (`ef9cf9a5`)
- lint (`8a493ed9`)
- **eslint**: refactor ESLint configuration across multiple packages (`4924a4c8`)
- **tsconfig**: standardize include and exclude patterns across configurations (`4ddbf590`)
- remove old schemas (`06c43985`)
- lint (`7cd534bc`)
- dump (`a68343b2`)
- update msw utils (`1b01b219`)
- update node engine version across packages (##105) (`315a422d`)
- update dependencies (`a1d2a3a7`)
- update package versions in pnpm-workspace.yaml (`9dde454c`)
- add Codecov configuration and badges to documentation (`e18b6d02`)
- format (`edd17fb7`)
- refactor tsdown configuration across packages (`323318de`)
- fix (`61a85457`)
- update Unicode API references and dependencies (`17ee2ee9`)
- update pnpm workspace configuration (`e0f3343e`)
- **utils**: remove `fs-extra` dependency and update documentation (`a824e6f7`)
- fix type (`2b72813b`)
- enhance type definitions for ensureDir, mkdir, stat, and rm methods in FSAdapter (`b014429a`)
- refactor createFileSystem and improve Node.js adapter handling (`c30a3720`)
- remove deprecated memfs-node adapter and update file system handling (`4d841493`)
- lint (`91e99158`)
- refactor FSAdapter methods and improve type definitions (`6772faad`)
- refactor file system adapter and update exports (`bff2921c`)
- apply coderabbit suggestion (`b23f96ce`)
- apply coderabbit suggestion (`dc8bd217`)
- apply coderabbit suggestion (`8661ad53`)
- apply coderabbit suggestion (`5f4e1e01`)
- add more tests (`1c276083`)
- lint (`fa61a58b`)
- use path.join (`1588f6ea`)
- add comment (`26fc64c6`)
- fix tests (`fb783972`)
- fix fs initialization (`a1465f7f`)
- add basePath to mirrorUCDFiles tests for custom API URL and pattern matching (`1ab7027c`)
- apply coderabbit suggestion (`372f8cd6`)
- fix types (`7c3d3392`)
- fix test (`ff9bcc24`)
- lint (`b91fe9ad`)
- lint (`d87c362c`)
- migrate to use tooling packages (`5b7cf43a`)
- update dependencies (`f262f3fc`)


