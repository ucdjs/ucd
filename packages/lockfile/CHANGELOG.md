# @ucdjs/lockfile v0.2.0
**Previous version**: `0.1.0`
**New version**: `0.2.0`


## ✨ Features
- **lockfile**: update exports for hash and lockfile functions (`aa9687ee`)
- **lockfile**: add tests for lockfile and snapshot creation (`2979832f`)
- **lockfile**: add createdAt and updatedAt timestamps to lockfile entries (`62b10760`)
- **lockfile**: add validation for lockfile schema and improve error handling (`962e69eb`)
- **lockfile**: implement Unicode header stripping and hashing (`861e45ae`)
- **lockfile**: add @luxass/utils to devDependencies (`6efd4278`)
- update dependencies and enhance lockfile path handling (`7d925743`)
- introduce @ucdjs/lockfile package for lockfile and snapshot management (`f6a22412`)

## 🐛 Bug Fixes
- **lockfile**: allow setting filter in lockfile builder (`106d4c75`)
- try to ensure correct lockfile generation (`92188aee`)
- **lockfile**: check mkdir capability correctly in writeSnapshot (`dcf0722f`)
- update package.json to remove duplicate dependency entry for @ucdjs-internal/shared (`5bd7ea2f`)

## ♻️ Refactoring
- reorganize pnpm catalogs for better scoping (##480) (`ba721776`)
- update type definitions and clean up imports across multiple files (`f7f602a2`)

## 🔧 Chores
- **workspace**: update dependencies and package manager (`1fcda2ca`)
- reorganize package catalogs and update dependencies (`ea2df11e`)
- update packageManager to pnpm@10.29.1 across all packages (`6bb966ab`)
- update dependencies and package manager (`e91a1ec4`)
- update dependencies (`59402701`)
- update pkg (`b4039996`)
- update dependencies (`51e6a071`)
- update typecheck command in package.json files (`34fa0ae7`)
- upgrade pnpm (`b06a7dd7`)
- update dependencies (`4b3590b9`)


