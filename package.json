{
  "name": "@ucdjs/monorepo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.13"
  },
  "packageManager": "pnpm@10.33.0",
  "workspaces": [
    "packages/*",
    "apps/*",
    "vscode"
  ],
  "scripts": {
    "build": "turbo run build --filter \"./packages/**\"",
    "build:apps": "turbo run build --filter \"./apps/*\"",
    "build:app": "turbo run build --filter \"./apps/${APP_NAME}\"",
    "build:vscode": "turbo run build --filter ./vscode",
    "clean": "turbo run clean && git clean -xdf node_modules",
    "dev": "turbo run dev --filter \"./packages/**\" --concurrency=30 --ui=tui",
    "dev:libs": "turbo run dev --filter \"./packages/*\" --concurrency=20 --ui=tui",
    "dev:pipelines": "turbo run dev --filter \"./packages/pipelines/**\" --concurrency=10 --ui=tui",
    "dev:apps": "turbo run dev --filter \"./apps/*\" --ui=tui",
    "dev:vscode": "turbo run dev --filter ./vscode",
    "lint": "turbo run lint --continue",
    "lint:fix": "turbo run lint --continue -- --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "typecheck": "turbo run typecheck",
    "typecheck:apps": "turbo run typecheck --filter \"./apps/*\"",
    "typecheck:app": "turbo run typecheck --filter \"./apps/${APP_NAME}\"",
    "release": "node ./scripts/release.ts prepare",
    "release:verify": "node ./scripts/release.ts verify",
    "release:publish": "node ./scripts/release.ts publish"
  },
  "devDependencies": {
    "@types/node": "catalog:types",
    "@ucdjs/release-scripts": "catalog:build",
    "@ucdjs/ucdjs-scripts": "workspace:*",
    "@vitest/ui": "catalog:test",
    "eslint": "catalog:lint",
    "turbo": "catalog:build",
    "typescript": "catalog:build",
    "vitest": "catalog:test"
  }
}
