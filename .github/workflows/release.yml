name: Release

on:
  push:
    branches:
      - '**'

env:
  TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
  TURBO_TEAM: ${{ secrets.TURBO_TEAM }}
  TURBO_TELEMETRY_DISABLED: 1
  DO_NOT_TRACK: 1
  FORCE_COLOR: true

permissions:
  contents: read

jobs:
  verify-fast:
    name: verify release (fast)
    runs-on: blacksmith-4vcpu-ubuntu-2404
    if: github.repository == 'ucdjs/ucd' && github.ref_name != 'release/next'
    permissions:
      contents: read
      statuses: write
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@fa2e9d605c4eeb9fcad4c99c224cee0c6c7f3594 # v2.16.0
        with:
          egress-policy: audit

      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: verify release (short-circuit)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh api \
            --method POST \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            /repos/${{ github.repository }}/statuses/${{ github.sha }} \
            -f state='success' \
            -f target_url='${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}' \
            -f description='Verification skipped' \
            -f context='ucdjs/release-verify'

  verify:
    name: verify release
    runs-on: blacksmith-4vcpu-ubuntu-2404
    if: github.repository == 'ucdjs/ucd' && github.ref_name == 'release/next'
    permissions:
      contents: read
      statuses: write
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@fa2e9d605c4eeb9fcad4c99c224cee0c6c7f3594 # v2.16.0
        with:
          egress-policy: audit

      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5.0.0

      - name: setup node
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          node-version: lts/*
          package-manager-cache: false

      - name: install dependencies
        run: pnpm install --frozen-lockfile

      - name: verify release
        run: pnpm release:verify
        env:
          UCDJS_RELEASE_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  release:
    name: release
    runs-on: blacksmith-4vcpu-ubuntu-2404
    if: |
      github.ref == 'refs/heads/main' &&
      github.repository == 'ucdjs/ucd' && (
        github.event.pusher.name == 'luxass' &&
        contains(github.event.head_commit.message, 'Merge pull request') &&
        contains(github.event.head_commit.message, 'release/')
      )
    permissions:
      contents: write
      pull-requests: read
      id-token: write
    environment:
      name: npm-registry
      deployment: false
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@fa2e9d605c4eeb9fcad4c99c224cee0c6c7f3594 # v2.16.0
        with:
          egress-policy: audit

      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5.0.0

      - name: setup node
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          registry-url: "https://registry.npmjs.org"
          node-version: lts/*
          package-manager-cache: false

      - name: update npm (oidc)
        run: npm install -g npm@latest

      - name: install dependencies
        run: pnpm install --frozen-lockfile

      - name: check packages for common errors
        run: pnpm turbo run build lint typecheck --filter "./packages/**"

      - name: create release
        run: pnpm release:publish
        env:
          UCDJS_RELEASE_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_CONFIG_PROVENANCE: true
